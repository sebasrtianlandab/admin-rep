# Core infrastructure

Reserved for cross-cutting technical adapters (HTTP client configuration, logging, etc.) once backend integration begins.

Do not place feature-specific mocks here — use `features/<name>/infrastructure/`.
